info:
  name: Current User Profile
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/auth/me"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
