info:
  name: Get All Privileges Tree View
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/privileges/tree"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
