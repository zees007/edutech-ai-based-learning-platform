info:
  name: Get All Privileges -Flat View
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/privileges"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
